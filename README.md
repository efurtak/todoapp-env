# Setup

```sh
git clone https://github.com/efurtak/todoapp-env.git
python3 -m venv todoapp-env/
cd todoapp-env/
source bin/activate
pip install -r requirements.txt
cd todoappprojectdir/
python manage.py runserver
```

# Go to URL

<http://localhost:8000/todo/>
